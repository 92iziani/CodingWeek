String uname = login.getText();
        String pass = password.getText();
        ResultSet rs;

        if (uname.equals("") && pass.equals("")) {
            Alert alert = new Alert(Alert.AlertType.INFORMATION);
            alert.setHeaderText("Champs vides ");
            alert.showAndWait();
            login.setText("");
            password.setText("");
            login.requestFocus();
        }

        try {
            Class.forName("org.sqlite.JDBC");
            connection = DriverManager.getConnection("jdbc:sqlite:src/sample/Database/data.db");
            pst = connection.prepareStatement("INSERT INTO users (uId, Prenom, Nom, Type, Email, Login, Password) VALUES(111, (?),(?),(?),(?),(?),(?));");
            pst.setString(1, nom.getText());
            pst.setString(2, prenom.getText());
            pst.setString(3, type.getText());
            pst.setString(4, email.getText());
            pst.setString(5, login.getText());
            pst.setString(6, password.getText());

            rs = pst.executeQuery();

            if (rs.next()) {

                System.out.println("found it");
                    /* Alert alert = new Alert(Alert.AlertType.INFORMATION);
                    alert.setHeaderText("Utilisateur trouvé! ");
                    alert.showAndWait(); */

            } else {

                System.out.println("not exist");
                Alert alert = new Alert(Alert.AlertType.INFORMATION);
                alert.setHeaderText("Mauvais exemple");
                alert.showAndWait();
                login.setText("");
                password.setText("");
                login.requestFocus();
            }
        } catch (Exception e) {
            System.out.println("" + e.getMessage());
        }
    }
